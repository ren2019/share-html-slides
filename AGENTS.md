# 纸飞机

## Agent skills

### Issue tracker

规格与任务使用 GitHub Issues。创建、读取、拆分或更新任务前，读取 `docs/agents/issue-tracker.md`。

### Triage labels

采用五个默认角色标签。分派任务状态前，读取 `docs/agents/triage-labels.md`。

### Domain docs

采用单一上下文。探索或修改领域行为前，读取 `CONTEXT.md` 和 `docs/agents/domain.md`。

## Product scope

产品与交互工作以 `docs/spec/phase-1-html-sharing.md` 为规格，主题以 `docs/brand/themes.md` 为准。历史对标探索仅供证据参考，不自动成为产品需求。

## Public repository

提交采用明确文件清单。环境密钥、原站会话证据、原始探索与合成探针留在本地，不提交到公开仓库。
