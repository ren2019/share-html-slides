'use client';
import { useEffect, useState } from 'react';
import Link from 'next/link';
import { useRouter } from 'next/navigation';
import { ArrowUpRight, Upload, Plus, Search, MoreHorizontal, Copy, FolderOpen, FileCode2, Link2, Check, ChevronDown, ArrowRight, Download, Pause, Play, Trash2, CircleCheck, CircleAlert, BookOpen, X, RotateCcw, Palette } from 'lucide-react';
import { Button } from '@/components/ui/button';
import { Dialog, DialogContent, DialogHeader, DialogTitle, DialogDescription, DialogFooter } from '@/components/ui/dialog';
import { DropdownMenu, DropdownMenuContent, DropdownMenuItem, DropdownMenuTrigger, DropdownMenuSeparator } from '@/components/ui/dropdown-menu';
import { Input } from '@/components/ui/input';
import { Badge } from '@/components/ui/badge';
import { Switch } from '@/components/ui/switch';
import { Progress } from '@/components/ui/progress';
import { OrigamiMark } from '@/components/origami-mark';
import { ShareDialog } from '@/components/share-dialog';
import { themes, initialMaterials, useLibrary, useThemeSync, setState, publishMaterials } from '@/lib/library';
import type { Material } from '@/lib/library';

export default function Page(){
 const router=useRouter();
 const {items,upload,stage,partial,theme}=useLibrary();
 useThemeSync();
 const [query,setQuery]=useState(''),[confirmPartial,setConfirmPartial]=useState(false),[share,setShare]=useState<Material|null>(null),[deleting,setDeleting]=useState<Material|null>(null),[toast,setToast]=useState(''),[filter,setFilter]=useState('all'),[lastFresh,setLastFresh]=useState(true);
 const filtered=items.filter(i=>`${i.title}${i.file}`.includes(query)&&(filter==='all'||(filter==='public'?!i.stopped:i.stopped)));
 useEffect(()=>{console.info('PAPERPLANE PROTOTYPE',{theme:themes[theme][0],stage,upload,partial,filter,query,items})},[theme,stage,upload,partial,filter,query,items]);
 useEffect(()=>{if(!toast)return;const t=setTimeout(()=>setToast(''),3200);return()=>clearTimeout(t)},[toast]);
 const start=()=>setState({stage:'choose',partial:false,upload:true});
 const shareItem=(i:Material)=>setShare(i);
 const openRead=(id:number)=>router.push(`/read/${id}`);
 const mainId=(items.find(i=>i.file==='增长策略.html')||items[0])?.id??3;
 const publish=(incomplete=false)=>{setLastFresh(!items.some(i=>i.file==='案例解读.html'));setConfirmPartial(false);setState({partial:incomplete,stage:'publishing'});setTimeout(()=>{publishMaterials(incomplete);setState({stage:'done'})},850)};
 const reset=()=>{setState({items:initialMaterials,stage:'choose',upload:false,partial:false});setQuery('');setFilter('all');setToast('演示已重置')};
 const resultFiles=partial?[['增长策略.html','从洞察到行动：增长策略分享']]:[['增长策略.html','从洞察到行动：增长策略分享'],['案例解读.html','案例解读'],['数据说明.html','数据说明']];
 return <div className="shell">
 <header className="site-header"><div className="header-inner"><Link href="/" className="brand" aria-label="纸飞机材料库"><OrigamiMark/><span>纸飞机<small>PAPERPLANE</small></span></Link><nav><span className="nav-current">材料库</span></nav><div className="header-actions"><DropdownMenu><DropdownMenuTrigger asChild><Button variant="ghost" className="theme-trigger"><Palette size={16}/>{themes[theme][0]}<ChevronDown size={13}/></Button></DropdownMenuTrigger><DropdownMenuContent align="end">{themes.map((t,i)=><DropdownMenuItem key={t[0]} onSelect={()=>setState({theme:i})}><span className="swatch" style={{background:`linear-gradient(135deg,${t[1]},${t[2]})`}}/>{t[0]}{theme===i&&<Check className="ml-auto" size={14}/>}</DropdownMenuItem>)}</DropdownMenuContent></DropdownMenu><Button className="brand-button header-upload" onClick={start}><Plus/>上传材料</Button><DropdownMenu><DropdownMenuTrigger asChild><Button variant="ghost" className="account"><span className="avatar">林</span><ChevronDown size={13}/></Button></DropdownMenuTrigger><DropdownMenuContent align="end"><div className="account-label">林知远<small>演示发布者 · 未连接账号</small></div></DropdownMenuContent></DropdownMenu></div></div></header>
 <main className="workspace"><section className="page-intro"><h1>材料库<span className="count">{items.length}</span></h1></section>
 <div className="library-toolbar"><div className="filters">{[['all','全部材料'],['public','分享中'],['stopped','已停止']].map(([v,l])=><button key={v} className={filter===v?'selected':''} onClick={()=>setFilter(v)}>{l}</button>)}</div><div className="search"><Search size={16}/><Input aria-label="搜索材料" placeholder="搜索标题或文件名" value={query} onChange={e=>setQuery(e.target.value)}/>{query&&<button aria-label="清除搜索" onClick={()=>setQuery('')}><X size={14}/></button>}</div></div>
 <section className="material-list" aria-label="材料列表"><div className="list-heading"><span>材料</span><span>版本</span><span>分享状态</span><span>首次上传</span><span>最后更新</span><span>操作</span></div>{filtered.map(item=><article className="material-row" key={item.id}><div className="material-main"><span className="file-tile"><FileCode2 size={23} strokeWidth={1.3}/><small>HTML</small></span><div><div className="material-id">NO. {String(item.id).padStart(3,'0')} <span> / {item.kind}</span></div><Link className="material-title" href={`/read/${item.id}`}>{item.title}</Link><div className="filename">{item.file}</div><div className="row-meta"><span className="meta-tags">r{item.revision} · {item.stopped?'已停止':'分享中'}</span><span className="meta-time"><b>首次上传</b>{item.created}</span><span className="meta-time"><b>最后更新</b>{item.updated}</span></div></div></div><span className="rev">r{item.revision}</span><div className="status"><span className={item.stopped?'status-dot stopped':'status-dot'}/>{item.stopped?'已停止':'分享中'}<small>{item.download?'允许下载':'下载已关闭'}</small></div><div className="date">{item.created}</div><div className="date">{item.updated}</div><div className="row-actions"><Button variant="ghost" size="sm" disabled={item.stopped} onClick={()=>shareItem(item)}><Link2 size={15}/>分享</Button><DropdownMenu><DropdownMenuTrigger asChild><Button variant="ghost" size="icon" aria-label={`管理${item.title}`}><MoreHorizontal size={19}/></Button></DropdownMenuTrigger><DropdownMenuContent align="end"><DropdownMenuItem onSelect={()=>openRead(item.id)}><ArrowUpRight/>打开阅读页</DropdownMenuItem><DropdownMenuItem disabled={item.stopped} onSelect={()=>shareItem(item)}><Copy/>分享文案</DropdownMenuItem><DropdownMenuSeparator/><DropdownMenuItem onSelect={e=>e.preventDefault()}><Download/>允许下载<Switch aria-label={`允许下载${item.title}`} checked={item.download} onCheckedChange={v=>setState({items:items.map(x=>x.id===item.id?{...x,download:v}:x)})}/></DropdownMenuItem><DropdownMenuItem onSelect={()=>{setState({items:items.map(x=>x.id===item.id?{...x,stopped:!x.stopped}:x)});setToast(item.stopped?'已恢复分享，原链接继续可用':'已停止分享，可随时恢复')}}>{item.stopped?<Play/>:<Pause/>}{item.stopped?'恢复分享':'停止分享'}</DropdownMenuItem><DropdownMenuSeparator/><DropdownMenuItem variant="destructive" onSelect={()=>setDeleting(item)}><Trash2/>永久删除</DropdownMenuItem></DropdownMenuContent></DropdownMenu></div></article>)}{!filtered.length&&<div className="empty"><BookOpen size={36} strokeWidth={1}/><h2>{query?'没有找到这份材料':'这里还没有材料'}</h2><p>{query?'换个标题或文件名试试。':'上传 HTML 文件以创建材料。'}</p><Button variant="outline" onClick={query?()=>setQuery(''):start}>{query?'清除搜索':'上传第一份材料'}</Button></div>}</section>
 <div className="library-note"><span><Link2 size={14}/> 同名上传，自动更新，分享链接保持不变。</span><button onClick={start}>上传材料 <ArrowRight size={14}/></button></div>
 </main>
 <div className="prototype-dock"><span className="prototype-indicator"/><span>交互原型</span><span className="dock-separator"/><span>合成材料 · 不会真实上传</span><Button variant="ghost" size="sm" onClick={reset}><RotateCcw size={13}/>重置</Button><details><summary>查看状态</summary><pre>{JSON.stringify({theme:themes[theme][0],stage,partial,filter,query,items},null,2)}</pre></details></div>
 {toast&&<div role="status" className="toast"><CircleCheck size={17}/>{toast}</div>}
 <Dialog open={upload} onOpenChange={v=>{if(stage!=='publishing')setState({upload:v})}}><DialogContent className="binding-dialog"><DialogHeader><DialogTitle>{stage==='done'?'发布完成':'上传材料'}</DialogTitle><DialogDescription>{stage==='done'?'每份关联 HTML 都是独立材料，可分别分享和管理。':'上传 HTML，检查关联文件后发布。'}</DialogDescription></DialogHeader>
 {stage==='choose'?<><button className="dropzone" onClick={()=>setState({stage:'missing'})}><span className="upload-symbol"><Upload size={27} strokeWidth={1.3}/></span><strong>选择 HTML 文件</strong><span>支持 HTML 幻灯片和长网页</span><span className="choose-button">选择演示材料 <ArrowUpRight size={14}/></span></button><div className="upload-guidance"><div><FolderOpen size={17}/><span>关联文件检查<small>自动列出需要补齐的图片、样式和页面。</small></span></div><div><RotateCcw size={17}/><span>同名文件更新<small>自动更新内容，保留原分享链接。</small></span></div></div><div className="fixture-label">本次演示：增长策略.html · 包含 2 份关联 HTML 与 3 个静态资源</div></>:stage==='done'?<><div className="success-banner"><CircleCheck size={26}/><div><strong>{partial?'1 份材料已更新':lastFresh?'1 份已更新，2 份已新建':'3 份材料已更新'}</strong><p>{partial?'缺失资源未包含，相关内容可能无法阅读。':'发布结果已同步至材料库。'}</p></div></div>{resultFiles.map(([f,t])=>{const m=items.find(i=>i.file===f);return <div className="result-row" key={f}><FileCode2 size={20}/><strong>{m?.title||t}</strong><Badge variant="secondary">{m&&m.revision>1?`已更新 · r${m.revision}`:'已新建'}</Badge><Button variant="ghost" size="sm" disabled={!m} onClick={()=>m&&shareItem(m)}><Link2/>分享</Button></div>})}<DialogFooter><Button className="brand-button" onClick={()=>setState({upload:false})}>返回材料库 <ArrowRight/></Button></DialogFooter></>:<><div className="queue-heading"><span>待发布 <b>3</b></span><Badge variant="outline">{stage==='missing'?'需要补齐':stage==='publishing'?'正在发布':'文件已齐'}</Badge></div><div className="queue"><div className="queue-main"><span className="file-tile small"><FileCode2 size={23}/></span><div><strong>增长策略.html</strong><small>同名材料已存在 · 原链接保持不变</small></div><Badge variant="secondary">更新</Badge></div><div className="relations"><span className="relation-label">发现关联 HTML · 发布后各自入库</span>{['案例解读.html','数据说明.html'].map((n,i)=><div className="relation-row" key={n}><FileCode2 size={17}/><div><strong>{n}</strong><small>{i?'嵌入页面':'普通链接'}</small></div><span className={stage==='missing'?'missing-label':'ready-label'}>{stage==='missing'?<CircleAlert size={14}/>:<Check size={14}/>}{stage==='missing'?'缺少文件':'已补齐'}</span></div>)}<div className="resource-row"><FolderOpen size={16}/>配图与样式<span>3 个资源 · {stage==='missing'?'待补齐':'已就绪'}</span></div></div></div>{stage==='missing'?<div className="missing-note"><CircleAlert size={18}/><div><strong>选择主文件所在文件夹</strong><p>只匹配这份内容需要的文件，无关文件不会上传。</p><Button variant="outline" size="sm" onClick={()=>setState({stage:'ready'})}><FolderOpen/>选择文件夹，补齐资源</Button></div></div>:<div className="ready-note"><CircleCheck size={17}/>文件已齐。关联材料可以独立更新、停止或删除。</div>}{stage==='publishing'&&<div className="publishing"><Progress value={65}/><span>正在发布演示材料…</span></div>}<DialogFooter className="binding-actions"><Button variant="ghost" disabled={stage==='publishing'} onClick={()=>openRead(mainId)}><BookOpen/>预览</Button><div>{stage==='missing'?<Button variant="ghost" className="publish-anyway" onClick={()=>setConfirmPartial(true)}>仍然发布</Button>:<Button className="brand-button" disabled={stage==='publishing'} onClick={()=>publish()}>{stage==='publishing'?'正在发布…':'发布 3 份材料'}<ArrowRight/></Button>}</div></DialogFooter></>}
 </DialogContent></Dialog>
 <Dialog open={confirmPartial} onOpenChange={setConfirmPartial}><DialogContent><DialogHeader><DialogTitle>仍然发布不完整内容？</DialogTitle><DialogDescription>还有 2 份关联 HTML 和 3 个静态资源未补齐。主材料将更新，部分内容可能无法阅读。</DialogDescription></DialogHeader><DialogFooter><Button variant="outline" onClick={()=>setConfirmPartial(false)}>返回补齐</Button><Button onClick={()=>publish(true)}>确认仍然发布</Button></DialogFooter></DialogContent></Dialog>
 {share&&<ShareDialog item={share} onClose={()=>setShare(null)} onFallback={setToast}/>}
 <Dialog open={!!deleting} onOpenChange={v=>{if(!v)setDeleting(null)}}><DialogContent><DialogHeader><DialogTitle>永久删除这份材料？</DialogTitle><DialogDescription>「{deleting?.title}」将被删除，原链接不可恢复。其他关联材料不受影响。</DialogDescription></DialogHeader><DialogFooter><Button variant="outline" onClick={()=>setDeleting(null)}>取消</Button><Button variant="destructive" onClick={()=>{setState({items:items.filter(i=>i.id!==deleting?.id)});setDeleting(null);setToast('已删除演示材料')}}>永久删除</Button></DialogFooter></DialogContent></Dialog>
 </div>
}
