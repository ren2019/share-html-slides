# 纸飞机 · 原站布局视觉原型

问题：保留对标站材料库行列表、顶部上传入口、上传检查发布弹窗的操作结构后，纸飞机的视觉与操作表达是否成立？用户要求只做一版，覆盖技能默认多方案要求。

运行：在本目录执行 `npm ci`，然后 `npm run dev`；访问 http://127.0.0.1:8877 。单独 Next.js 原型工程，不进入正式应用入口。

真实依赖：Next.js、React、Tailwind CSS、Lucide React、Radix UI。`src/components/ui` 来自 shadcn 官方 new-york-v4 registry，2026-09-13 拉取；替换本地 import 路径并翻译关闭按钮。来源 https://ui.shadcn.com/r/styles/new-york-v4/{组件名}.json ，MIT 许可附后。

可体验：主题（localStorage 记住当前浏览器选择，新浏览环境默认松烟墨）、列表搜索和状态筛选、上传演示文件、同名更新提示、发现关联、模拟目录补齐、独立 URL 详情阅读页（/read/[id]，列表标题、行内菜单与上传预览均进入，可返回且恢复上传队列）、完整与缺失发布、分享编辑/复制、停止/恢复、下载开关、删除确认与重置。材料含首次上传、最后更新与 revision，同名更新保留首次时间并递增 revision。每次状态变化输出 console，底部可展开状态。

无真实上传、登录、存储、文件解析或下载；复制按钮会按用户点击复制合成文案。没有调用对标站服务。页面内有明确原型提示。品牌标志使用用户选定的「01 经典折纸」参考图原图（public/brand/origami-logos-source.png，源图未改动），浏览器 CSS 裁切第一个图标并按主题做滤镜着色，白色折面保持原样。

布局依据：本地原站实测记录的顶部材料库导航/上传入口、材料行列表、上传检查发布弹窗、行内更多菜单。本次新匿名会话访问原站跳转登录，未取得当前登录页后截图；因此不声称像素级还原。已确认移除合集/收藏，更新入口统一同名上传；筛选、搜索的表现是原型建议。

Verdict：待用户视觉与操作评审。此原型不证明微信兼容或真实业务通过。不合入 main；此前 A/B/C 仅留作历史探索。

浏览器检查已完成：完整补齐→预览→模拟更新一份/新建两份→分享弹窗；缺失确认→只更新主材料；停止材料恢复分享；主题切换。默认初始数据已重置供评审。TypeScript 检查通过。尚未做微信真机验收；响应式样式已写，手机外观未独立验收。

React Doctor：86/100，0 errors，4 warnings（原型单页分支复杂、shadcn button/badge 的变体导出、progress 的 transition-all）。源码核对后保留：前者为可丢弃原型的集中状态实现，后者为上游组件写法；不为原型引入结构重构。这些提示不作为生产代码豁免。

用户视觉修订：移除三个标注说明及所有界面“装订”措辞，取消口号和纸张装饰，采用紧凑的企业工具密度。统一使用上传、检查关联文件、发布。已浏览器检查列表与上传弹窗，TypeScript 通过。

2026-09-13 主题与标志修订：五个主题统一为语义 CSS 变量（--brand/-light/-ink/-soft/-soft-border/-ring），经 `<html data-theme>` 应用，Radix portal 同步；覆盖选中导航、筛选选中态、链接悬停、焦点环、dropzone 悬停、队列与结果徽章浅色底、文本选中色，成功/警告/危险语义保持独立。标志改用手选「01 经典折纸」参考图原图，CSS 裁切第一个图标，按主题用 CSS filter 着色，飞机折面与白色保持原样。Playwright 截图检查五个主题首页、上传弹窗与分享弹窗，无控制台错误，TypeScript 通过。

2026-09-14 详情页与版本信息修订：阅读从弹窗改为独立 URL 详情页 /read/[id]，材料与上传队列状态移入模块级共享 store，客户端导航不丢状态，预览后返回恢复上传队列。列表改为 材料/版本/分享状态/首次上传/最后更新/操作 六列，时间为可核对的真实日期时间，同名更新保留首次时间、更新最后时间并 revision+1，重复发布继续递增。移除 800/500px 隐藏 date/status 的策略，≤860px 改为行内标注堆叠保留全部关键信息。Playwright 33 项断言通过（1440/823/390 三视口、直达 URL、返回恢复、revision 递增），TypeScript 通过。

2026-09-14 #2-#4 实施（阅读体验 / 认证 / 发布失败）：

- #2 阅读：`/read/[id]` 幻灯片真翻页（按钮 + ←/→，页码 k/N）、长网页真滚动 + 页内锚点；正文首屏含真实 `r{revision} · 更新时间`。关联：普通链接进目标阅读页；嵌入用父页合成 `srcDoc` iframe（sandbox=""，无脚本），关联停止/删除/未发布时局部提示、其余正文可读。加载骨架；`?fail=1` 触发加载失败 + 重试。未登录直达为读者视角（无材料库入口，仅“发布者登录”）；下载入口按开关提供，弹窗列未包含项及原因，含微信保存回退说明，复制失败保留可选取完整链接。
- #3 认证：`/login` 三 tab（登录 / 注册 / 找回密码，可独立审查）。登录含微信扫码分支（待扫描/确认/取消/过期刷新/失败重试，均有“模拟”按钮）与手机号密码（预填合成账号 10000000000 / demo123，勿输入真实信息；密码 wrong 演示失败）。注册与找回先短信验证（演示验证码 246810，30 秒有效，30 秒重发等待，可模拟过期）再设密码，未验证禁设密码。页底“演示部署配置”可关闭微信/短信入口；两者均关则注册登录整体不开放。材料库未登录时上传与管理跳登录；登录后账户菜单可退出。
- #4 发布失败：选择文件后“分析中”→ 缺失态逐项列出 2 份关联 HTML、3 个本地静态资源（assets/cover.png 等）与 2 项外部资源获取失败（保留原引用）；目录只匹配所需文件。ready 发布前弹“外部缺失确认”（区别于 missing 的“仍然发布”只发主文件）。演示场景可选“案例解读.html 发布失败”或“主材料更新失败”：逐份混合结果、失败项标“旧版 rN 继续可读/未创建”并可重试（成功后 revision 递增、时间更新、不重复创建、新 ID 单调不复用）；结果页持续列出未保存外部资源；已停止材料结果分享按钮禁用。dock“操作失败演示”开启后，停止/恢复/删除/下载设置均失败且状态不变，关闭后重试即恢复。

验证：`npm run typecheck`、`npm run build` 通过；`.scratch/implement-2-4/verify-2-4.cjs` 44 项断言全 PASS（含 1440/823/390 视口、匿名直达、SPA 状态链路）。React Doctor 复跑 86/100，回到基线；剩余 4 项为既有上游写法与原型集中状态（page.tsx 单页分支、shadcn button/badge 变体导出、progress transition-all），源码核对后保留，不作为生产代码豁免。

模拟与待验边界：全部为合成数据与内存态（整页刷新即复位，预览/返回走客户端导航保状态）；无真实登录、短信、上传、存储与下载；认证与微信交互（扫码、微信内保存、复制回退）不构成真机验收，需后续真机验证。

2026-09-14 匿名入口修正：匿名访问根 `/` 展示产品首页 `LandingHome`（`src/components/landing-home.tsx`）——直接说明“发布 HTML，分享阅读链接”、关联检查/同名更新稳定链接/读者免登录三要点、开源自托管说明（链接真实仓库 ren2019/share-html-slides）与“当前为交互原型”标注；主 CTA“登录并上传”→ /login；两张公开演示材料卡 → /read/3（幻灯片）、/read/2（长网页）。首页不展示私人材料列表、搜索、上传队列、管理菜单与状态 dump，保留五主题切换。登录后 `/` 仍为本人材料库；退出登录立即回产品首页并关闭残留上传弹窗。匿名 `/read/:id` 保持直达免登录；匿名态登录页返回链接为“返回首页”。验证：主代理 `.scratch/anonymous-home/verify.mjs` 24 项断言全 PASS（三尺寸、390px 登录后账户/退出入口、登录/退出闭环）；`npm run typecheck`、`npm run build` 通过；React Doctor 86/100 基线（主题菜单抽为共用 `src/components/theme-menu.tsx`，文案按自托管现状修正）。

2026-09-14 匿名阅读外壳修订（参考实测分享页层级，不复制其标志/文案/正文）：`/read/[id]` 改为独立阅读外壳——48px 轻量吸顶品牌栏（品牌回首页，滚动可用），桌面正文居中限宽 960px、浅灰底白色内容容器，手机 390/375 边距收紧到 12–16px、操作触达 ≥40px、含 safe-area 与锚点 scroll-margin，不加遮挡正文的底栏。正文前突出材料标题，次级“由 演示发布者 分享 · 更新于 …”使用合成发布者名（不取浏览者账号冒充作者）；文件名/编号/revision/首次时间等管理信息收进“材料信息”折叠区，材料库字段与正文内版本示例不变。分享为顶栏主要动作，允许下载时保留下载；“发布者登录”从顶栏移至页尾“使用纸飞机发布内容”/“了解纸飞机”，已登录仍可在顶栏（窄屏图标化）与页尾返回材料库。分享弹窗增加微信帮助说明（窗口右上角「···」可转发，或复制文案），统一展示、不做 UA 识别；复制失败保留可选完整文案，下载回退不变。HTML 示例正文样式/颜色/重排未动，幻灯片翻页、长文滚动、关联链接与 sandbox iframe、停止/删除/失败重试均保留；主题只影响外壳。验证：`.scratch/reader-reference/verify-reader.cjs` 23 项断言全 PASS（1440/390/375 视口、吸顶、折叠展开、翻页、失败重试、无横向溢出），登录态顶栏/页脚返回入口定向检查通过；`npm run typecheck`、`npm run build` 通过；React Doctor 86/100 基线无新增。主代理 CUA 复验 390 与 1440 阅读壳、翻页、材料信息展开与分享提示正常。未做微信真机验证，不构成微信兼容结论。

2026-09-14 阅读外壳细化（refinement 轮）：1) 删除阅读页外层 reader-head 标题/分享者/更新时间，HTML 长文与幻灯片直接呈现正文（正文自带标题与版本示例不变），“材料信息”折叠保留。2) “使用纸飞机”改为真实按钮 CTA：匿名桌面顶栏放实心按钮 → 产品首页 `/`；≤500px 顶栏 CTA 隐藏，页尾改放实心按钮（不固定、不遮正文），桌面页尾保留“了解纸飞机”文字链；登录态返回材料库入口不变。3) 下载环境识别：客户端检测 `wxwork`（企业微信优先）与 `MicroMessenger`（微信），仅这两类容器点“下载文件包”弹出全屏引导（同一 DialogContent 切换为全屏无 transform 样式，单一 role=dialog），箭头指向视口右上角，文案“点击右上角 ···，选择在浏览器中打开”，含“我知道了”与 Escape/关闭按钮；`?browser=wechat|wecom` 可强制并标注“模拟微信/企业微信环境”，普通路径无模拟标记、保留模拟下载 toast。下载弹窗不再展示微信通用说明与防复制声明；复制下载链接及失败回退、关联未包含列表保留。验证：`npm run typecheck`、`npm run build` 通过；React Doctor 86/100 基线四项不变；浏览器验收由主代理 CUA 执行（`.scratch/reader-reference/verify-reader.cjs` 为上一轮脚本，其中 reader-head 相关断言已不适用）。未做微信/企微真机验证，识别仅为 UA 判断。

本轮主代理 CUA 验收：桌面正文仅保留 HTML 自带标题、顶栏产品按钮可见；390×844 微信/企业微信模拟引导覆盖完整视口（x=0、y=0、width=390、height=844），我知道了返回下载选项、Escape 关闭正常；普通浏览器下载显示演示结果。构建与类型检查通过，Doctor 86/100 无新增。
